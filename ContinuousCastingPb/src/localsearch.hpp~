
#ifndef LOCALSEARCH_H
#define LOCALSEARCH_H

#include "Problem.h"

class LocalSearch {
protected:
  Problem probleme;
  int nbEval;
  double bestFitness;
  Solution s;
  double fitnessMax;
public:
  LocalSearch();
  explicit LocalSearch(Problem p);
  virtual void run(Solution s);
};
#endif //LOCALSEARCH_H
